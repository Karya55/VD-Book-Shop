<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.janfranco.bookstore">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BookStore">
        <activity android:name=".user_interface.activities.CommunityActivity"></activity>
        <activity android:name=".user_interface.activities.BookActivity" />
        <activity android:name=".user_interface.activities.CartActivity" />
        <activity android:name=".user_interface.activities.ProfileActivity" />
        <activity android:name=".user_interface.activities.HomeActivity" />
        <activity android:name=".user_interface.activities.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".user_interface.activities.RegisterActivity" />
        <activity android:name=".user_interface.activities.AuthActivity" />
    </application>

</manifest>